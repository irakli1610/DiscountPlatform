@model offers.itacademy.ge.Application.Models.ProductOffers.ProductOfferResponseModel

@{
    ViewData["Title"] = "Cancel Product Offer";
}

<div class="container">
    <h2 class="mt-4">Cancel Product Offer</h2>
    <p>Are you sure you want to cancel this offer? This is only possible within a specific time limit after creation.</p>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <dl class="row">
        <dt class="col-sm-2">Image</dt>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.ImageUrl))
            {
                <img src="/uploads/offers/@Model.ImageUrl" alt="@Model.Name" style="max-height: 100px;" />
            }
            else
            {
                <span>No Image</span>
            }
        </dd>
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Status</dt>
        <dd class="col-sm-10">@Model.Status</dd>
    </dl>

    <form asp-action="CancelConfirmed" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Cancel Offer</button>
        <a asp-action="MyOffers" class="btn btn-secondary ms-2">Back</a>
    </form>
</div>