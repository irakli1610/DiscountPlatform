@model IEnumerable<offers.itacademy.ge.Application.Models.Users.Admin.AdminResponseModel>

@{
    ViewData["Title"] = "Manage Admins";
    int pageNumber = ViewBag.PageNumber ?? 1;
    int pageSize = ViewBag.PageSize ?? 10;
}

<div class="container">
    <h2 class="mt-4">Manage Admins</h2>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="3">No admins found.</td>
                </tr>
            }
            else
            {
                @foreach (var admin in Model)
                {
                    <tr>
                        <td>@admin.Id</td>
                        <td>@admin.UserName</td>
                        <td>@admin.Email</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    <nav aria-label="Page navigation" class="mt-3">
        <ul class="pagination justify-content-end">
            <li class="page-item @(pageNumber == 1 ? "disabled" : "")">
                <a class="page-link" asp-action="Admins" asp-route-pageNumber="@(pageNumber - 1)" asp-route-pageSize="@pageSize">Previous</a>
            </li>
            <li class="page-item @(Model.Count() < pageSize ? "disabled" : "")">
                <a class="page-link" asp-action="Admins" asp-route-pageNumber="@(pageNumber + 1)" asp-route-pageSize="@pageSize">Next</a>
            </li>
        </ul>
    </nav>
</div>